{
  "name": "nhom4_baitap",
  "version": "1.0.0",
  "description": "Authentication API with User, OTP, Profile management",
  "main": "server.js",
  "scripts": {
    "dev": "npm run build:frontend && nodemon server.js",
    "start": "npm run build:frontend && node server.js",
    "setup": "cd frontend && npm install",
    "build:frontend": "cd frontend && npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.0",
    "joi": "^18.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.3",
    "nodemailer": "^8.0.7",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}

